<!DOCTYPE html>
<html>
  <head>
    <title>Dreamlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", rel: "stylesheet" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js", defer: true %>
    <link href="https://fonts.googleapis.com/css2?family=Creepster:wght@400&family=Nosifer:wght@400&family=Butcherman:wght@400&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  </head>

  <body class="text-white">
    <% 15.times do %>
      <div class="firefly"></div>
    <% end %>

    <!-- Notifications Container -->
    <%= notification_container %>

    <!-- Flash Messages as JSON for JavaScript processing -->
    <%= render_flash_notifications %>

    <!-- Analysis Loader -->
    <%= render "shared/analysis_loader" %>

    <!-- Zone de contenu (scroll libre) -->
    <div class="content-zone">
      <div class="main-content w-100 d-flex flex-column justify-content-center align-items-center py-4" style="z-index: 1; position: relative;">
        <%= yield %>
      </div>

      <!-- Include flash notifications handler -->
      <%= javascript_include_tag "flash_notifications", defer: true %>

      <!-- Custom Title Modal -->
      <%= render "shared/title_modal" %>
    </div>

    <!-- Zone navbar -->
    <div class="navbar-zone">
      <%= render("shared/bottom_nav") unless @hide_bottom_nav %>
    </div>
  </body>
</html>
